#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    my_list = []
  for num in range(len(my_list)):
    if num % 2 == 0:
      my_list.append(true)
  else:
  my_list.append(false)

  return my_list
